pub fn process<'a>(
    _input: &'a str,
) -> u32 {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_process() {
        let lines = "<input>";
        let result = process(lines);
        assert_eq!(result, 8);
    }
}
